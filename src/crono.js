/*
export function actualizarCronometro () {
  // Obtener la fecha y hora actual
  const ahora = new Date()

  // Calcular la diferencia de tiempo desde el inicio del cronómetro
  const diferencia = ahora - tiempo

  // Calcular horas, minutos, segundos y milisegundos
  const horas = Math.floor(diferencia / 3600000)
  const minutos = Math.floor((diferencia % 3600000) / 60000)
  const segundos = Math.floor((diferencia % 60000) / 1000)
  // eslint-disable-next-line no-unused-vars
  const milisegundos = diferencia % 1000

  // Formatear el tiempo en formato HH:MM:SS
  const tiempoFormateado = `${horas.toFixed(2)}:${minutos.toFixed(2)}:${segundos.toFixed(2)}`

  // Actualizar el contenido del elemento HTML con el tiempo formateado
  setTiempo(tiempoFormateado)
}
*/
